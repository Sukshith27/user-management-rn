const express = require('express');
const app = express();
const PORT = 4000;

// ...existing code from reference...

app.listen(PORT, () => {
  console.log(`Mock server running on port ${PORT}`);
});
